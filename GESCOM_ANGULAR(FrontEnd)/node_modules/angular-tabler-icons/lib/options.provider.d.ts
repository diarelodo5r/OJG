import { InjectionToken } from '@angular/core';
import { Options } from './options.interfaces';
export declare const OPTIONS_TOKEN: InjectionToken<Options[]>;
export declare class OptionsProvider {
    private options;
    constructor(options: Options[]);
}
