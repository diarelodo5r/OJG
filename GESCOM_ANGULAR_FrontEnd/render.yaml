services:
  - type: web
    name: gescom-angular-frontend
    env: static
    buildCommand: npm ci && npx ng build --configuration=production
    staticPublishPath: dist/materialm
    routes:
      - type: rewrite
        source: /*
        destination: /index.html
